Drop your exported `needs_practice_*.jsonl` file(s) here (from the app: Revision → "Export to dataset/needs_practice").
Each record already carries its own sectionId/topicId/topic, so no folder-based organisation is needed here — just drop
the file(s) in and run `npm run dataset:build && npm run dataset:validate`.
